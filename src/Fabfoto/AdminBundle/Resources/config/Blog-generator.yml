generator: admingenerator.generator.doctrine
params:
  model: Fabfoto\GalleryBundle\Entity\ArticleBlog
  namespace_prefix: Fabfoto
  bundle_name: AdminBundle
  fields:
    content:
      formType: genemu_tinymce
      addFormOptions:
        required: false 
    tags:
      formType: entity
      addFormOptions:
        class: "FabfotoGalleryBundle:Tag"
        multiple: true
        required: false
builders:
  list:
    params:
      title: List of Blog articles
      display: [id, createdAt, updatedAt, title, subtitle, author ]
      actions:
        new: ~
      object_actions:
        edit: ~
        delete: ~
  filters:
    params:
      display: [ createdAt, updatedAt, title, subtitle, author ]
  new:
    params:
      title: New object for AdminBundle
      display: [ title, subtitle, content, author, tags ]
      actions:
        list: ~
  edit:
    params:
      title: You're editing the object "{{ ArticleBlog.title }}"
      display: [ title, subtitle, content, author, tags ]
      actions:
        list: ~
  delete: ~
