{% macro linkNavBar(image, url, title ) %}
 <a href="{{ url }}" rel="tooltip" title="{{ title | trans }}" class="helper" >
    <div class="visible-desktop">
        <i class="{{image}} icon-black"></i>
    </div>
    <div class="hidden-desktop">
        <i class="{{image}} icon-black"></i> 
        {{ title | trans }}
    </div>
</a>
{% endmacro %}
