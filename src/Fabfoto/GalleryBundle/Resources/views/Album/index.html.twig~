{% extends 'FabfotoGalleryBundle::layoutAdmin.html.twig'%}
{% block content %}
<h1>Administration albums </h1>
<ul data-role="listview" data-filter="true">

    {% for entity in entities %}
    <li>
        <a href="{{ path('adminAlbums_show', { 'id': entity.id }) }}">
            {% if entity.picturecover.thumbpath %} <image src="/{{entity.picturecover.thumbpath}}" class ="image_index_admin"/>{% endif%}
            <h3>{{ entity.name }}</h3>
            <p>{{ entity.comment }}</p>
            <p>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif%}</p>
        </a>
    </li>
    {% endfor %}

</ul>
<a href="{{ path('adminAlbums_new') }}" data-role="button">
    Create a new entry
</a>
{% endblock content %}
